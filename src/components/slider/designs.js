const designsImage = {
  1: Array.from({ length: 5 }, (_, i) => `/images/${11 + i}.webp`),
  2: Array.from({ length: 3 }, (_, i) => `/images/${21 + i}.webp`),
  3: Array.from({ length: 3 }, (_, i) => `/images/${31 + i}.webp`),
  4: Array.from({ length: 4 }, (_, i) => `/images/${41 + i}.webp`),
  5: Array.from({ length: 2 }, (_, i) => `/images/${51 + i}.webp`),
  6: Array.from({ length: 4 }, (_, i) => `/images/${61 + i}.webp`),
  7: Array.from({ length: 7 }, (_, i) => `/images/${71 + i}.webp`),
  8: Array.from({ length: 3 }, (_, i) => `/images/${81 + i}.webp`),
};

export default designsImage;
